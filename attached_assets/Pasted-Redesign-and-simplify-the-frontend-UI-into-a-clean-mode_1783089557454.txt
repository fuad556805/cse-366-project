Redesign and simplify the frontend UI into a clean, modern single-page interface inspired by the attached reference design.

UI Design Requirements:
- Use the attached screenshot as the visual design reference.
- Create a modern, minimal, professional dashboard.
- Use a clean black & white color scheme with subtle gray borders and shadows.
- Add generous spacing, rounded corners, and a simple responsive layout.
- Center the main content with a maximum width similar to the reference.
- Keep typography clean and easy to read.
- The UI should look polished and professional.

Functional Requirements:
- Remove the entire multi-step wizard (Upload, Preview, Visualize, Target, Train, Results, Predict).
- Remove all progress indicators and navigation between steps.
- Keep everything on a single page.

Layout:
1. A page title such as "Natural Language to SQL".
2. A short subtitle describing the application.
3. A large drag-and-drop CSV upload area similar to the reference design.
4. After a CSV is uploaded, display:
   - a single natural language question input box,
   - an "Ask" button beside or below the input.
5. When the user clicks "Ask":
   - show the generated SQL query,
   - then display the query results in a clean table directly below.
6. If no CSV is uploaded, hide the question input and results section.

Design Style:
- Large upload card similar to the reference image.
- Dashed upload border.
- Simple upload icon.
- White background.
- Light gray borders.
- Rounded cards.
- Minimal animations.
- Fully responsive for desktop, tablet, and mobile.
- No unnecessary colors or visual clutter.

Important:
- Do NOT modify any backend logic, APIs, routes, database, SQL generation pipeline, or application functionality.
- Only simplify and redesign the frontend UI.
- Keep all existing backend functionality exactly as it is.
- Focus only on improving the user interface and user experience while preserving the current system behavior.